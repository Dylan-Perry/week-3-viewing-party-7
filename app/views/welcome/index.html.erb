<% if current_user %>
    <section class='existing-users'>
        <p>Existing Users: </p>
        <% @users.each do |user| %>
            <li><%= user.email %></li>
        <% end %>
    </section>
<% else %>
    <%= button_to "Create New User", register_path, method: :get %>
<% end %>
